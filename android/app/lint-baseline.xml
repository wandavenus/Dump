<?xml version="1.0" encoding="UTF-8"?>
<!--
  Lint baseline — generated by ./gradlew :app:lintDebug -Dlint.baselines.continue=true
  Captures all lint issues present at baseline creation time. Future CI runs fail only
  on issues NOT listed here (i.e. new issues introduced by subsequent commits).

  To regenerate after intentional changes:
    cd android && ./gradlew :app:lintDebug -Dlint.baselines.continue=true
  Then commit the updated file.

  This file is intentionally empty until the first real baseline is generated.
  If CI fails on first run, download the 'lint-reports' artifact, replace this
  file with the included lint-baseline.xml, and commit the result.
-->
<issues format="6" by="lint 8.7.3">

</issues>
