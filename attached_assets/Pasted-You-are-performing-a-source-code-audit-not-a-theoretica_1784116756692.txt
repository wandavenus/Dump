You are performing a source-code audit, not a theoretical review.

Current state:

- The project builds successfully.
- SignalsmithStretchAudioProcessor compiles.
- Flutter speed and pitch sliders work.
- Kotlin logs confirm setSpeed() and setPitchSemitones() are called.
- The processor is installed into the Media3 AudioProcessor chain.
- However, changing speed or pitch has absolutely NO audible effect.

Your task is to find the real root cause using ONLY evidence from the source code.

Rules:

1. Do NOT guess.
2. Do NOT list possibilities.
3. Every conclusion must be backed by actual code.
4. Quote the exact file, function, and relevant lines.
5. If evidence is missing, explicitly state what file is required instead of inventing explanations.
6. Do not stop after auditing Kotlin. Continue into the native C++ implementation.

Audit the entire pipeline from UI to DSP:

Flutter UI
↓
MethodChannel
↓
TransportCommands
↓
StretchManager
↓
SignalsmithStretchAudioProcessor
↓
Media3PlaybackService
↓
DefaultAudioSink
↓
AudioProcessorChain
↓
JNI
↓
stretch_native.cpp
↓
signalsmith-stretch.h
↓
Processed PCM output

Answer the following.

━━━━━━━━━━━━━━━━━━━━━━
1. Processor instance identity
━━━━━━━━━━━━━━━━━━━━━━

Verify whether the SAME SignalsmithStretchAudioProcessor instance receiving queueInput() is also the one updated by:

- setSpeed()
- setPitchSemitones()

Show the object creation path and lifetime.

━━━━━━━━━━━━━━━━━━━━━━
2. AudioProcessor installation
━━━━━━━━━━━━━━━━━━━━━━

Verify the processor is actually installed into the active ExoPlayer instance.

Show:

- DefaultAudioSink.Builder()
- DefaultAudioProcessorChain(...)
- processor order

━━━━━━━━━━━━━━━━━━━━━━
3. Configuration
━━━━━━━━━━━━━━━━━━━━━━

Trace onConfigure().

Verify whether it can return AudioFormat.NOT_SET.

If yes,

show exactly why,

including:

- library availability
- encoding
- channel count
- sample rate

Quote the code.

━━━━━━━━━━━━━━━━━━━━━━
4. queueInput()
━━━━━━━━━━━━━━━━━━━━━━

Audit queueInput() carefully.

Verify:

- ByteBuffer position
- ByteBuffer limit
- replaceOutputBuffer()
- output sizing
- frame calculations

IMPORTANT:

Determine whether this code is correct:

inputBuffer.position(inputBuffer.limit())

before nativeProcess().

If nativeProcess() relies on ByteBuffer position/remaining, prove whether this empties the buffer.

Do not speculate.

Use the JNI implementation as evidence.

━━━━━━━━━━━━━━━━━━━━━━
5. Native implementation
━━━━━━━━━━━━━━━━━━━━━━

Audit the native C++ implementation.

Read:

- stretch_native.cpp
- every JNI function
- any helper classes

Verify:

nativeCreate()

nativeDestroy()

nativeReset()

nativeProcess()

nativeFlush()

nativeSetPitchSemitones()

nativeOutputLatencyFrames()

For each function explain exactly what it does.

━━━━━━━━━━━━━━━━━━━━━━
6. Signalsmith API usage
━━━━━━━━━━━━━━━━━━━━━━

Verify that the JNI actually calls Signalsmith Stretch correctly.

Show:

- Stretch object construction
- process()
- setTransposeSemitones()
- latency handling

Compare the implementation against the upstream Signalsmith Stretch API.

If usage is incorrect,
identify the exact incorrect call.

━━━━━━━━━━━━━━━━━━━━━━
7. Speed implementation
━━━━━━━━━━━━━━━━━━━━━━

Verify that speed is actually applied.

Trace:

speed slider
↓

Kotlin

↓

JNI

↓

Signalsmith

↓

PCM output

Determine whether outputFrames are calculated correctly.

Determine whether process() is invoked using the intended stretch ratio.

━━━━━━━━━━━━━━━━━━━━━━
8. Pitch implementation
━━━━━━━━━━━━━━━━━━━━━━

Verify that pitch actually reaches the native object.

Trace:

pitch slider
↓

Kotlin

↓

JNI

↓

Signalsmith object

↓

process()

Verify that nativeSetPitchSemitones() updates the SAME native handle later used by nativeProcess().

━━━━━━━━━━━━━━━━━━━━━━
9. Audio output
━━━━━━━━━━━━━━━━━━━━━━

Determine whether processed samples are actually written into the output ByteBuffer.

Check:

- memcpy
- direct buffer writes
- float conversion
- output limits

Verify whether output audio is ever replaced by the processed data.

━━━━━━━━━━━━━━━━━━━━━━
10. CMake
━━━━━━━━━━━━━━━━━━━━━━

Audit:

CMakeLists.txt

Verify:

- stretch_native.cpp is compiled
- no obsolete JNI implementation exists
- correct source files are linked
- correct include directories are used

━━━━━━━━━━━━━━━━━━━━━━
11. PlaybackParameters
━━━━━━━━━━━━━━━━━━━━━━

Verify there is NO code that bypasses Signalsmith by using:

PlaybackParameters

SonicAudioProcessor

SpeedChangingAudioProcessor

or another speed processor.

Quote every relevant location.

━━━━━━━━━━━━━━━━━━━━━━
12. Final verdict
━━━━━━━━━━━━━━━━━━━━━━

Provide ONLY evidence-based conclusions.

For every finding use this format:

STATUS:
VERIFIED / NOT VERIFIED

Evidence:
(file, function, quoted code)

Reasoning:
Explain why the evidence proves the conclusion.

━━━━━━━━━━━━━━━━━━━━━━
13. Root cause
━━━━━━━━━━━━━━━━━━━━━━

If the root cause is found:

show

- the exact offending code
- why it breaks playback
- the minimal patch
- why that patch fixes the issue

If the evidence is still insufficient,

DO NOT GUESS.

Instead state exactly which additional file(s) are required to continue the investigation.