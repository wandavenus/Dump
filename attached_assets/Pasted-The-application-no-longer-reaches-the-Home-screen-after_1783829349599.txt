The application no longer reaches the Home screen after Phase 9. It remains stuck indefinitely on the item/loading screen.

Please perform a complete startup audit.

Do NOT assume the problem is FFmpeg itself. Trace the entire startup flow from application launch until the Home screen is displayed.

Audit all initialization dependencies, including but not limited to:

- main.dart
- AudioService.initialize()
- PlaybackManager.initialize()
- Media3PlaybackBridge.initialize()
- FfmpegDecoderBridge.initialize()
- FfmpegCapabilityProbe
- NativeAudioRuntime initialization
- Native DSP initialization
- MethodChannel registrations
- EventChannel subscriptions
- Any Future.wait(), Completer, Stream.first, await chains, or initialization guards.

Look specifically for:

1. Any Future that never completes.
2. Any synchronous startup dependency introduced in Phase 9.
3. Any MethodChannel invokeMethod() waiting forever.
4. EventChannel listeners that are awaited before the first event is emitted.
5. Deadlocks between PlaybackManager and AudioService.
6. Startup code that blocks the Home screen until FFmpeg capability probing completes.
7. Missing MethodChannel handler registration.
8. Missing EventSink initialization.
9. Any exception that is swallowed inside async initialization.
10. Any timeout that should exist but does not.

The FFmpeg capability probe MUST be fail-open.

If FFmpeg is unavailable, initialization must immediately continue and report:

- ffmpegAvailable = false

instead of blocking application startup.

The Home screen must NEVER depend on FFmpeg initialization.

If FFmpeg probing is currently awaited during startup, move it to a background asynchronous task after the application is fully initialized.

Please identify the exact blocking point, explain why the startup hangs, and implement the safest fix while preserving the Phase 9 architecture.

Finally provide:

- Root cause
- Files modified
- Why the Home screen was blocked
- Why the fix is correct
- Any remaining startup risks