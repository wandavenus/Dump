# Native Runtime Final Cleanup

Read:

- Native_Code_Audit_Verification_Report_2026_07_19.md

The verification report concludes that only two findings remain.

Your task is to verify them again against the CURRENT codebase before making any changes.

Do not blindly apply the recommendations.

---

## NEW-01

File:

native_audio_runtime/src/comp_processor.c

Reported issue:

Potential divide-by-zero when:

knee_db == 0.0

Current code reportedly allows:

```c
const float t = (over + knee_half) / p->knee_db;
```

Requirements:

1. Verify the execution path.

2. Determine whether the division can actually occur.

3. Confirm whether:

```c
if (!isfinite(gain_linear))
    gain_linear = 1.0f;
```

already guarantees fail-open behaviour.

4. If the issue is still valid:

choose the safest fix.

Preferred solutions:

Option A

Clamp:

```c
knee_db >= 0.01f
```

during parameter validation.

Option B

Treat:

```c
knee_db <= epsilon
```

as a hard-knee compressor and avoid the division entirely.

Choose whichever produces the cleanest DSP behaviour.

Do NOT change compression curves unless necessary.

---

## NEW-02

File:

native_audio_runtime/src/dsp_pipeline.c

Reported issue:

nar_dsp_pipeline_reset()

does not verify:

```c
_initialized
```

before iterating slots.

Requirements:

1. Verify whether a real race is possible.

2. Trace:

- initialization
- reset
- dispose
- audio thread
- service shutdown

3. If the guard is purely defensive but harmless,

add:

```c
if (!atomic_load(&_initialized))
    return;
```

only if it cannot change runtime behaviour.

Do not introduce extra locking.

---

## Validation

After changes:

Run static review.

Confirm:

- no new warnings
- no behaviour changes
- no ABI changes
- no DSP behaviour changes

---

## Deliverables

For each finding provide:

Status:

- Fixed
- Not needed
- False positive

Explain why.

Show:

- exact file
- exact function
- exact lines changed

Finally provide:

"Native runtime cleanup complete."

Only modify code if the issue is still valid after verification.