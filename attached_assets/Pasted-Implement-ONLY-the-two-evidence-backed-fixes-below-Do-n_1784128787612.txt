Implement ONLY the two evidence-backed fixes below.

Do not refactor.
Do not optimize.
Do not change the DSP architecture.
Do not touch Flutter or TransportCommands.

The speed/pitch pipeline is already working correctly.

Fix only these two issues.

────────────────────────────────────────

1. Never skip flushing based on current speed/pitch.

Current code incorrectly assumes:

(speed == 1 && pitch == 0)
==
"there is no buffered STFT state"

This is false.

If the processor was active earlier, Signalsmith may still contain buffered output even after speed/pitch return to unity.

Replace this logic with proper state tracking.

Requirements:

- Track whether the Stretch engine has processed any non-bypass audio since the last reset/flush.
- Flush at EOS whenever buffered state may exist.
- Reset the flag after a successful flush/reset.
- Do not flush unnecessarily on a processor that never entered STFT mode.

Do NOT use current speed/pitch as the decision.

────────────────────────────────────────

2. Improve EOS flush quality.

Signalsmith documentation states outputLatency() is only the minimum recommended flush size.

Using only outputLatency() can trigger the library's fold-back path.

Instead:

- Query the library for its preferred full drain size (block/window size if available).
- If no public API exists, derive the safest available size from the existing implementation.
- Use the larger safe value instead of outputLatency() alone.

The goal is a clean linear drain with no unnecessary tail folding.

────────────────────────────────────────

Rules

- Do NOT introduce mutexes.
- Do NOT change JNI signatures.
- Do NOT modify the speed algorithm.
- Do NOT modify carryFrames.
- Do NOT modify the playback pipeline.
- Do NOT change ToFloat/ToInt16 placement.

After implementation:

1. Explain exactly what changed.
2. Explain why each change is correct according to the Signalsmith source.
3. Confirm no behavior changed except EOS/bypass flushing.
4. Run analysis/build verification and report any issues.