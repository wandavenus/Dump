Proceed with a full production-hardening implementation.

Implement every issue identified in the audit.

Do not change public APIs unless absolutely necessary.

Maintain binary compatibility wherever possible.

Priorities (highest first):

────────────────────────────────────────

1. Fix the DSP crossfade data race.

Current implementation incorrectly shares mutable DSP state across multiple concurrent NativeDspAudioProcessor instances.

Implement per-stream runtime state.

Requirements:

- No mutexes in the audio callback.
- No heap allocation in the audio callback.
- No std::mutex.
- No malloc/new during processing.
- No additional audio latency.
- No behavior changes outside concurrent playback.

Prefer using the existing `void* self` / pipeline instance mechanism if practical.

If that is not sufficient, implement a compile-time fixed per-stream state model.

Every stateful processor must have isolated runtime state, including:

- ReplayGain
- Loudness Normalizer
- Compressor
- Limiter
- PEQ
- Crossfeed
- every processor that stores mutable runtime variables

Verify there is zero shared mutable runtime state between simultaneously playing streams.

────────────────────────────────────────

2. Add NaN / Infinity protection everywhere.

Audit every DSP processor.

Ensure every processor sanitizes:

- NaN
- +Inf
- -Inf
- denormals

before values can propagate.

One corrupted processor must never permanently poison downstream state.

────────────────────────────────────────

3. Sample-rate correctness.

Audit every processor using attack/release coefficients or filter coefficients.

Whenever sample rate changes:

- regenerate coefficients
- regenerate smoothing constants
- regenerate envelope constants

Ensure timing is identical at:

44.1
48
88.2
96
176.4
192 kHz

────────────────────────────────────────

4. Pipeline robustness.

Current behavior aborts processing when one processor returns an error.

Refactor so that:

- one processor failure bypasses only that processor
- limiter and safety stages always execute
- downstream processors continue whenever safe

Never leave playback without clipping protection because an upstream processor failed.

────────────────────────────────────────

5. LoudnessEnhancer interlock.

Verify interaction between:

ReplayGain
Loudness Normalizer
Android LoudnessEnhancer

Prevent double loudness processing.

Document the precedence rules.

────────────────────────────────────────

6. Runtime verification.

Stress-test mentally and by code inspection:

- crossfade
- gapless
- seek
- pause
- resume
- rapid next
- rapid previous
- enable/disable DSP while playing
- sample-rate switching
- mono
- stereo
- multichannel
- silence
- clipped masters
- podcasts
- classical music

────────────────────────────────────────

7. Performance verification.

Confirm:

- no new allocations inside audio callback
- no locks inside audio callback
- no additional JNI crossings
- no unnecessary memcpy
- no measurable latency increase

────────────────────────────────────────

8. Documentation.

Update comments wherever architecture changed.

Explain:

- why runtime state became per-stream
- why parameters remain shared
- why the solution is realtime-safe

────────────────────────────────────────

9. Final validation.

Run flutter analyze.

Review every modified file for regressions.

Do not leave TODOs.

Do not leave temporary debug logging.

Finally provide:

- list of modified files
- explanation of every architectural change
- confirmation that realtime safety has been preserved
- confirmation that no heap allocation occurs in the audio callback
- confirmation that no mutex exists in the audio callback
- confirmation that concurrent crossfade streams are fully isolated