# Phase 3 — Performance Optimization

## Objective

Implement ONLY the validated performance improvements from `Audit_Validation_Report.md`.

The objective is to:

- reduce unnecessary allocations
- reduce rebuilds
- reduce I/O overhead
- improve scrolling performance
- improve playback responsiveness

without changing application behavior.

Do NOT perform architectural refactors.

Do NOT redesign existing systems.

---

# Source of Truth

Use ONLY:

- Audit_Validation_Report.md

Ignore:

- Rejected
- False Positive
- Needs Manual Verification (NMV)

Implement ONLY validated findings.

---

# Scope

## 1. ThemeController

Review:

- ThemeController._save()

Current issue:

- SharedPreferences.getInstance() is retrieved repeatedly.

Improve by:

- caching the SharedPreferences instance
- avoiding repeated initialization

Maintain identical behavior.

---

## 2. HistoryService

Review:

- repeated SharedPreferences writes
- repeated getInstance()

Optimize safely.

Do NOT change save semantics.

---

## 3. PlaylistService

Review JSON decoding.

Current issue:

- JSON decode occurs on every read.

Reduce unnecessary decoding where safe.

Preserve synchronization.

---

## 4. AudioSession

Review:

- AudioSession.instance

Avoid repeated retrieval.

Cache safely.

---

## 5. Player progress rebuilds

Review:

- progress updates
- ValueListenableBuilder
- StreamBuilder
- AnimatedBuilder

Reduce rebuild scope.

Never reduce update accuracy.

---

## 6. ThemeData allocation

Review ThemeData creation.

Move immutable objects outside build() whenever possible.

Preserve runtime theme switching.

---

## 7. Album & Artist cards

Review:

- FutureBuilder usage
- artwork loading
- palette loading

Reduce duplicate work.

Avoid changing UI.

---

## 8. ArtworkRepository

Improve validated cache behavior.

Examples:

- stale entries
- deleted file handling
- cache lookup efficiency

Do not redesign cache architecture.

---

## 9. Lyrics cache

Review:

- duplicated cache
- cache lookup
- repeated allocations

Implement only validated optimizations.

---

## 10. LogService

Review:

- List.removeAt(0)

If appropriate:

replace with a more efficient FIFO implementation.

Preserve ordering.

---

## 11. Theme text styles

Reduce duplicated TextStyle allocations.

Avoid changing typography.

---

## 12. Build optimizations

Review validated rebuild issues:

Examples:

- unnecessary map().toList()
- repeated DateTime.now()
- repeated allocations
- unnecessary widget creation
- avoidable object creation inside build()

Only optimize validated cases.

---

## 13. Const optimization

Apply validated const improvements where safe.

Do NOT force const if runtime values exist.

---

## 14. Rendering optimization

Review validated rendering issues.

Examples:

- BackdropFilter
- progress rebuilds
- FutureBuilder usage
- unnecessary repaint

Only implement validated improvements.

---

# Safety Rules

Do NOT:

- split large files
- rename folders
- redesign services
- change UI behavior
- modify playback logic
- change DSP logic

Performance improvements only.

---

# Validation

Before completion:

Run:

- flutter analyze

Verify:

- application launches
- scrolling remains smooth
- playback unchanged
- no analyzer warnings introduced
- no performance regression

---

# Deliverables

## Summary

For every validated performance finding:

- Fixed
- Skipped

Explain why.

---

## Benchmarks

Where measurable, report improvements such as:

- fewer allocations
- reduced rebuilds
- reduced I/O
- reduced repeated object creation

Do not invent numbers.

Only report measured improvements.

---

## Changed Files

List every modified file.

---

## Remaining Work

List only findings that belong to:

- Phase 4 (Architecture & Refactor)
- Phase 5 (Cleanup)

Stop after Phase 3.

Do not continue automatically.