# Phase 6 — Safe-to-Fix Implementation

## Objective

Implement ONLY the findings listed in the **SAFE** and **SAFE WITH CONDITIONS** sections of:

- Revalidation_Report_2026_07_18.md

This is NOT another audit.

This is an implementation phase.

The repository has already been re-validated.

Do not search for additional issues.

---

# Source of Truth

Use ONLY:

- Revalidation_Report_2026_07_18.md

Ignore:

- REQUIRES DESIGN REVIEW
- DO NOT FIX

Do not implement anything outside the Safe-to-Fix Checklist.

---

# Rules

Every fix must satisfy all of the following:

- preserve existing behavior
- preserve playback
- preserve Media3
- preserve ReplayGain
- preserve DSP
- preserve background playback
- preserve public APIs

No architecture changes.

No file splitting.

No redesign.

---

# Implement SAFE findings

## Playlist

Fix:

- widget.userPlaylist! force unwrap

Use proper null handling.

Do not silently ignore errors.

---

## Media3

Replace validated

catch (_) {}

blocks with:

- logging
- safe fallback

Never swallow exceptions silently.

---

## Playlist model

Replace:

- unsafe casts

Examples:

(json['songIds'] as List)

(json['createdAt'] as int)

with safe parsing.

---

## Lyrics

Replace

providerName.contains("tag")

with a more robust implementation.

Do not change runtime behavior.

---

## Scroll updates

Reduce unnecessary

setState()

calls during scrolling.

Maintain identical UI.

---

## HistoryService

Reduce repeated SharedPreferences writes.

Keep save semantics identical.

---

## Timer lifecycle

Cancel timers before reassignment.

---

## Rate limiter

Replace DateTime.now()

with a more appropriate timing mechanism.

---

## AudioSession

Cache AudioSession.instance safely.

Avoid repeated initialization.

---

## StreamSubscription

Fix every validated subscription leak.

Cancel safely.

Never double cancel.

---

## Playlist cache

Avoid repeated decode where already validated.

---

## Player Song Info

Remove:

- empty Align
- redundant force unwraps

---

## Player More Menu

Replace silent catch blocks.

---

## Lyrics settings

Add cleanup for timer map.

---

## Shared TextStyle

Extract duplicated TextStyle.

Do not change appearance.

---

## Duplicate widgets

Merge validated duplicate widgets.

Only if behavior remains identical.

---

## Navigation

Await validated Navigator.push calls.

---

## Edge-to-edge

Move validated applyEdgeToEdge()

out of build()

without changing behavior.

---

## DSP

Improve validated error handling.

Do not change DSP behavior.

---

## Low priority fixes

Implement:

- const improvements
- DecoratedBox replacement
- map().toList() optimization
- Color.lerp optimization
- null-safe chains

Only where explicitly validated.

---

# SAFE WITH CONDITIONS

Implement ONLY if all required conditions are satisfied.

Examples:

## initializeAll()

Parallelize ONLY modules that have no dependency ordering.

Otherwise keep sequential.

---

## BackdropFilter

Disable GPU filter entirely

when effect is effectively invisible.

Maintain identical visuals.

---

## Artwork cache

Add bounded cache.

Do not reduce cache hit rate.

---

## LRC offset

Verify all supported LRC formats before modifying offset handling.

---

## ScrollPosition

Replace unsafe casts only if behavior remains identical.

---

# Validation

Before completion:

Run:

- flutter analyze

Verify:

- application builds
- playback works
- DSP works
- ReplayGain works
- background playback works
- analyzer clean

---

# Deliverables

## Summary

For every Safe finding:

- Fixed
- Skipped

Explain why.

---

## Changed Files

List every modified file.

---

## Validation

Report:

- flutter analyze
- build status

---

## Deferred

List every item intentionally left for:

- Design Review
- Do Not Fix

Do not implement them.

Stop after Phase 6.