You are performing a root-cause investigation, NOT proposing theories.

Current symptom:
- The app builds successfully.
- SignalsmithStretchAudioProcessor compiles.
- Flutter slider updates speed/pitch correctly.
- Kotlin logs confirm setSpeed() and setPitchSemitones() are called.
- However, changing speed or pitch has absolutely no audible effect.

Your task is to find the REAL root cause.

Rules:
1. Do NOT guess.
2. Do NOT list possibilities.
3. Every conclusion MUST be backed by evidence from the actual source code.
4. Quote the exact file, class, function, and relevant lines that prove your conclusion.
5. If evidence is insufficient, explicitly say what additional file is required instead of inventing explanations.

Audit the entire chain:

Flutter UI
↓
MethodChannel / JNI
↓
StretchManager
↓
SignalsmithStretchAudioProcessor instance(s)
↓
Media3PlaybackService
↓
DefaultAudioSink.Builder(...)
↓
ExoPlayer
↓
AudioProcessor chain
↓
nativeProcess()
↓
Signalsmith C++

Verify each step with evidence.

Specifically answer:

1. Is the SAME SignalsmithStretchAudioProcessor instance receiving queueInput() also the one updated by setSpeed()/setPitchSemitones()?

2. Is SignalsmithStretchAudioProcessor actually installed into the ExoPlayer AudioProcessor chain?

3. Does queueInput() execute during playback?

4. Does nativeProcess() execute during playback?

5. Is nativeSetPitchSemitones() called on the same native handle used by nativeProcess()?

6. Is handle recreated after playback starts, causing parameter updates to target a stale handle?

7. Does any PlaybackParameters, SonicAudioProcessor, SpeedChangingAudioProcessor, or another AudioProcessor bypass or replace Signalsmith?

8. Is the processor inactive because onConfigure() returns AudioFormat.NOT_SET?

9. Is the processor configured for PCM_FLOAT while playback is actually PCM_16BIT or another encoding?

For every answer provide:

- VERIFIED or NOT VERIFIED
- Source file
- Exact code proving it
- Why that evidence proves the conclusion

If the root cause is found, provide:
- the exact offending code,
- why it breaks playback,
- and the minimal patch.

Do not stop at the first issue.
Continue until the complete processing chain has been verified.