# Phase 1 — Critical & High Priority Fixes

## Objective

Implement **ONLY** the validated **Critical** and **High** priority findings from `Audit_Validation_Report.md`.

Do **NOT** perform additional refactors.
Do **NOT** change unrelated code.
Do **NOT** introduce architectural changes.

The goal of this phase is to improve correctness and stability while keeping the patch as small and safe as possible.

---

# Source of Truth

Use **ONLY**:

- Audit_Validation_Report.md

Ignore all rejected findings.

Ignore all findings marked:

- Rejected
- False Positive
- Needs Manual Verification (NMV)

Only implement findings that are explicitly marked **Valid**.

---

# Scope

## Critical

### 1. Missing Flutter assets

Fix missing assets in `pubspec.yaml`.

Include every validated asset reported by the audit.

Verify:

- flutter pub get succeeds
- release build will include them

---

## High

Implement only these validated issues.

### 1. Fix "Putih" color bug

File:

- lyrics_pickers.dart

Current behavior:

- "Putih" incorrectly maps to `Colors.black`.

Fix:

- map it to the correct white color.

---

### 2. Fix unsafe array indexing

Files:

- albums_section/card.dart

Current code contains:

- cached[2]
- colors[2]

Prevent:

- RangeError
- invalid index access

Use safe bounds checking.

---

### 3. Remove unsafe force unwraps

Fix validated force unwraps:

- ModalRoute.of(context)!
- widget.userPlaylist!
- widget.smartType!
- _current!
- playback_engine stats!

Replace with safe null handling while preserving behavior.

Do not silently ignore failures.

---

### 4. Native database collision risk

File:

- MetadataCacheDb.kt

Current implementation:

- path.hashCode()

Replace with a collision-safe key strategy.

Migration must preserve existing functionality.

---

### 5. ActivePlayerProxy state mismatch

File:

- CrossfadeController.kt

Implement the validated fix so that:

- currentTimeline
- currentPeriodIndex

remain synchronized.

---

### 6. widget_test.dart

Replace Flutter default counter template.

Either:

- create meaningful smoke tests

or

- remove obsolete template if intentionally unused.

---

### 7. Folder naming issue

Investigate:

lib/Bottom NavBar/

If safe:

rename to a conventional folder name.

Update:

- imports
- exports
- references

If renaming is risky, explain why and skip.

---

### 8. God files

DO NOT split them in Phase 1.

Instead:

Insert clear TODO markers indicating future refactor boundaries.

No large refactor yet.

---

### 9. ThemeData allocation

If ThemeData is recreated every build:

Move immutable portions outside build().

Maintain identical UI behavior.

---

# Requirements

Every fix must:

- compile
- pass analyzer
- preserve behavior
- avoid regressions
- avoid unnecessary formatting-only changes

---

# Validation

Before finishing:

- flutter analyze
- build verification
- verify there are no new analyzer issues

---

# Deliverables

Provide:

## 1. Summary

For every finding:

- Fixed
- Skipped
- Not Applicable

Explain why.

---

## 2. Changed Files

List every modified file.

---

## 3. Risks

Mention any behavior changes.

---

## 4. Remaining Work

List findings that belong to Phase 2 and beyond.

---

Do not continue into Phase 2 automatically.

Stop immediately after Phase 1 is complete.