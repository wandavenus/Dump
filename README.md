# Music Player

A Flutter music player app with browsing, radio, library, search, and local playback screens.

## Getting Started

1. Install Flutter and the platform toolchains for Android or iOS.
2. Fetch dependencies with `flutter pub get`.
3. Run the app with `flutter run`.

## Testing

Run the test suite with:

```sh
flutter test
```
